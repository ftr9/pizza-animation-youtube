import { TWENTY_PER_OF_SC_WIDTH } from '../../../constants/ContentsWidth';
import { View } from 'react-native';

const ListDummyComponent = () => {
  return (
    <View
      style={{
        width: TWENTY_PER_OF_SC_WIDTH,
      }}
    ></View>
  );
};

export default ListDummyComponent;
